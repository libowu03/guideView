<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_weight="1"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:gravity="center"
    android:layout_gravity="center"
    app:layout_constraintBottom_toBottomOf="parent">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textSize="16dp"
        android:textStyle="bold"
        android:id="@+id/calendarDay"
        android:gravity="center"
        tools:text="21"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textColor="@color/currentMonthColor"
        android:text="21" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:textSize="13dp"
        tools:text="十二"
        app:layout_constraintTop_toBottomOf="@id/calendarDay"
        android:id="@+id/calendarFestivalOrLunar"
        app:layout_constraintLeft_toLeftOf="@id/calendarDay"
        app:layout_constraintRight_toRightOf="@id/calendarDay"
        android:textColor="@color/currentMonthColor"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        tools:text="休"
        android:id="@+id/calendarHolidayStatus"
        app:layout_constraintLeft_toRightOf="@id/calendarDay"
        app:layout_constraintBottom_toBottomOf="@id/calendarDay"
        android:textColor="@color/colorRed"
        android:textSize="8sp"/>
</androidx.constraintlayout.widget.ConstraintLayout>